# Image Manipulator Environment Variables

# Required for OCR functionality
OPENROUTER_API_KEY=sk-or-v1-your-api-key-here

# Optional: Concurrency for OCR processing (default: 1, max: 5)
OCR_CONCURRENCY=2

# Optional: Server port (Railway auto-assigns if not set)
PORT=3000

# Optional: Default image directory
IMAGE_DIR=

# Optional: Node environment
NODE_ENV=production
