# Image Manipulator environment configuration
# Copy this file to `.env` and fill in the required values before running the server.

# --- Required ---
# OpenRouter API key that unlocks the real OCR pipeline. Get one at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# --- Optional overrides ---
# Port the Express server should listen on (defaults to 3001 if unset)
# PORT=3001

# Path to the directory that contains the images you want to process.
# If omitted you can set it from the UI later.
# IMAGE_DIR=C:\\path\\to\\images

# OCR tuning knobs (safe defaults already exist)
# OCR_DEFAULT_MODEL=openai/gpt-4o-mini
# OCR_TIMEOUT=30000
# OCR_MAX_RETRIES=3
